---
apiVersion: v1
kind: Secret
metadata:
  name: minio-credentials
  namespace: immich
type: Opaque
stringData:
  ACCESS_KEY_ID: "{{ minio_access_key }}"
  ACCESS_SECRET_KEY: "{{ minio_secret_key }}"
